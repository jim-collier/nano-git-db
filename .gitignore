# Build output
/bin/
/dist/
*.exe

# Local SQLite view (derived from the tx-log; rebuildable)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Test/coverage
*.out

# Donation signing keys live outside the repo (../private/donation_keys/); never
# let a stray copy land in git.
donation_ed25519
donation_keys/

# Build output
/bin/
/dist/
*.exe

# Local SQLite view (derived from the tx-log; rebuildable)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Test/coverage
*.out

# Donation signing key: lives outside the repo (../private/donation_keys/); never
# let a stray copy land in git. The detached signature (donation.sig) is tracked.
donation_ed25519
donation_keys/

# Build output
/bin/
/dist/
*.exe

# Local SQLite view (derived from the tx-log; rebuildable)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Test/coverage
*.out

# CI/CD artifacts (run logs, flamegraphs) - derived, rotated in place
/cicd/artifacts/

# Donation signing keys live outside the repo (../private/donation_keys/); never
# let a stray copy land in git.
donation_ed25519
donation_keys/
__pycache__/

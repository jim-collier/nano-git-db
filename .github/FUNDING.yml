# GitHub reads this to show a "Sponsor" button on the repo. Fill in the handles
# you use and uncomment them. An empty or not-yet-created handle 404s, so keep a
# platform commented until its account exists. The custom link below always works,
# since it points at DONATE.md in this repo.

# github: [jim-collier]      # uncomment once GitHub Sponsors is enabled
# ko_fi:
# liberapay:
# patreon:
custom: ["https://github.com/jim-collier/nano-git-db/blob/main/DONATE.md"]
